name: prod_analytical_env
version: 1.0.0
profile: prod_analytical_env
config-version: 2

# -----------------------------
# FOLDER → SCHEMA & MATERIALIZATION
# -----------------------------
models:
  prod_analytical_env:  # MUST match project name
    source:
      +schema: SALES
      +materialized: view  # all source models as view
    intermediate:
      +schema: SALES
      +materialized: view  # intermediate models as view
    mart:
      +schema: SALES
      +materialized: table  # mart models as table

# -----------------------------
# DEFAULT VARIABLES
# -----------------------------
