name: snowflake_analytics
version: "1.0"
config-version: 2

profile: Roshan Kumar Vats

models:
  snowflake_analytics:
    +schema: "{{ 'sales' if target.name == 'prod_analytical_env' else 'rvats' }}"